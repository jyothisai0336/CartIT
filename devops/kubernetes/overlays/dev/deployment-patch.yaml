apiVersion: apps/v1
kind: Deployment
metadata:
  name: cartit-frontend
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: cartit-frontend
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
