apiVersion: apps/v1
kind: Deployment
metadata:
  name: cartit-frontend
spec:
  replicas: 5
  template:
    spec:
      containers:
        - name: cartit-frontend
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
